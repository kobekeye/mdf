Bundled from `typst/typst-assets` tag `v0.13.1`.

This directory intentionally vendors only the minimal Typst text/math font set
used by the extension's offline preview path. See the accompanying `LICENSE`
and `NOTICE` files from the upstream asset bundle.
